# EnGarde Subscription Sync Service - Environment Variables

# Service Configuration
PORT=8000
ENV=development  # development or production

# Database Connections
# Langflow Database (En-Garde-FlowDB) - for templates and flows
LANGFLOW_DATABASE_URL=postgresql://user:pass@host:port/langflow_database

# EnGarde Main Database (Postgres) - for subscription tiers and walker agents
ENGARDE_DATABASE_URL=postgresql://user:pass@host:port/engarde_database

# Service-to-Service Authentication
# Shared secret token - EnGarde backend must include this in Authorization header
SUBS_SYNC_SERVICE_TOKEN=your_secure_random_token_here

# Logging
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR
